import 'package:flutter/material.dart';

class AboutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text(
            "Hydroponic System is used to view the status of the hydroponic plants and informs the user of any abnormalities in the variables of the plants"),
      ),
    );
  }
}
